{
  "name": "@playground/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for shared auth integration across tool apps",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
